name: Bootstrap and validate Escudo

on:
  push:
    paths:
      - "bootstrap/**"
      - ".github/workflows/bootstrap.yml"
      - "bootstrap_payload.b64"
  pull_request:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  validate-source:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Reassemble and verify source archive
        shell: bash
        run: |
          set -euo pipefail
          cat bootstrap/payload.part* > /tmp/escudo-source.tar.gz.b64
          base64 --decode /tmp/escudo-source.tar.gz.b64 > /tmp/escudo-source.tar.gz
          echo "8a52c949bf82d8e927e23d2cb483d6bd2f794356e292c3a66f951981ed7d2799  /tmp/escudo-source.tar.gz" | sha256sum --check
          mkdir -p /tmp/escudo-source
          tar -xzf /tmp/escudo-source.tar.gz -C /tmp/escudo-source

      - name: Apply CI-discovered v0.9 fixes
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path

          root = Path("/tmp/escudo-source")
          bridge = root / "app/src/main/java/com/escudo/app/clone/CloneBridgeService.kt"
          text = bridge.read_text()
          text = text.replace("import android.os.Process\n", "")
          text = text.replace("import android.os.UserHandle\n", "")
          begin = text.index("    private fun enforceEscudoCaller() {")
          finish = text.index("\n    private fun scheduleStop()", begin)
          replacement = """    private fun enforceEscudoCaller() {
              val callerPackages = packageManager.getPackagesForUid(Binder.getCallingUid()).orEmpty()
              check(packageName in callerPackages) { "Caller no autorizado" }
          }
          """
          text = text[:begin] + replacement + text[finish:]
          text = text.replace(
              "the calling appId matches Escudo's appId, so possession of the Binder alone is not enough for an",
              "the calling UID resolves to Escudo's package, so possession of the Binder alone is not enough for an"
          )
          bridge.write_text(text)

          check = root / "tools/security-regression-check.sh"
          lines = check.read_text().splitlines()
          dq = chr(34)
          replacement_check = "grep -q 'getPackagesForUid(Binder.getCallingUid())' " + dq + "$BRIDGE_SERVICE" + dq + " || fail " + dq + "el Binder debe verificar que cada IPC provenga del paquete Escudo" + dq
          lines = [
              replacement_check
              if "UserHandle.getAppId" in line and "$BRIDGE_SERVICE" in line
              else line
              for line in lines
          ]
          check.write_text("\n".join(lines) + "\n")

          provisioning = root / "app/src/main/java/com/escudo/app/policy/ProvisioningController.kt"
          text = provisioning.read_text()
          text = text.replace(
              "    val isManagedProfile: Boolean\n        get() = Build.VERSION.SDK_INT >= Build.VERSION_CODES.N && userManager.isManagedProfile\n",
              """    val isManagedProfile: Boolean
        get() = if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.R) {
            userManager.isManagedProfile
        } else {
            // UserManager.isManagedProfile() only became public in API 30. In Escudo's
            // provisioning model, a non-device Profile Owner is the managed vault profile.
            isProfileOwner && !isDeviceOwner
        }
"""
          )
          provisioning.write_text(text)

          android_ci = root / ".github/workflows/android.yml"
          android_ci.write_text(
              android_ci.read_text().replace(
                  "android-actions/setup-android@v3",
                  "android-actions/setup-android@v4"
              )
          )

          assert "UserHandle.getAppId" not in bridge.read_text()
          assert "getPackagesForUid(Binder.getCallingUid())" in bridge.read_text()
          PY

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - uses: android-actions/setup-android@v4

      - name: Install Android SDK 35
        run: |
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager "platforms;android-35" "build-tools;34.0.0" "platform-tools"

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Core self-test
        working-directory: /tmp/escudo-source
        run: ./tools/core-self-test.sh

      - name: Security regression check
        working-directory: /tmp/escudo-source
        run: ./tools/security-regression-check.sh

      - name: Unit tests
        working-directory: /tmp/escudo-source
        run: gradle :app:testDebugUnitTest --stacktrace

      - name: Android lint
        working-directory: /tmp/escudo-source
        shell: bash
        run: |
          set +e
          gradle :app:lintDebug :demo-target:lintDebug --stacktrace
          status=$?
          echo "===== app lint report ====="
          find app/build -name 'lint-results-debug.txt' -print -exec cat {} \; 2>/dev/null || true
          echo "===== demo-target lint report ====="
          find demo-target/build -name 'lint-results-debug.txt' -print -exec cat {} \; 2>/dev/null || true
          exit "$status"

      - name: Build APKs
        working-directory: /tmp/escudo-source
        run: gradle :app:assembleDebug :demo-target:assembleDebug --stacktrace

      - uses: actions/upload-artifact@v4
        with:
          name: escudo-v0.9-debug-apks
          path: |
            /tmp/escudo-source/app/build/outputs/apk/debug/*.apk
            /tmp/escudo-source/demo-target/build/outputs/apk/debug/*.apk

      - name: Expand project onto main
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        shell: bash
        run: |
          set -euo pipefail
          find . -mindepth 1 -maxdepth 1 ! -name .git -exec rm -rf {} +
          cp -a /tmp/escudo-source/. .
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "Import Escudo Android MVP v0.9"
          git push origin HEAD:main
